module.exports = ({ env }) => ({
  // Esempio: plugin upload (se serve)
  // upload: {
  //   config: {
  //     provider: 'local',
  //     providerOptions: {
  //       sizeLimit: 10000000,
  //     },
  //   },
  // },
});
